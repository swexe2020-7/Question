<h1>QUESTION Toppage</h1>
<p>
    こちらは質問投稿サイトです!!!
</p>
    <% if current_user %>
     <%= link_to 'ログアウト', logins_logout_path, class: "CRUD" %>
     <% else %>
    <%= link_to 'ログイン' , logins_login_form_path, class: "CRUD"%>
     <% end %>
    <%= link_to '新規登録' , new_user_path, class: "CRUD"%>
    <%= link_to '検索' , homes_new_path, class: "CRUD"%>
    <%= link_to '投稿' , homes_new_path, class: "CRUD"%>
</p>
<table>
    <% @homes.each do |t| %>
    <tr>
    <tb><%= t.message %> </tb>
    <% time = ((Time.current.to_i - t.tdate.to_i) / 60) %>
     
     <%= "#{time}分前" %>
    <br>
     <%= link_to '削除', home_path(t.id), method: :delete, class: "CRUD" %>
     <%= link_to '表示', home_path(t.id), class: "CRUD" %>
     <%= link_to '編集', edit_home_path(t.id), class: "CRUD" %>
     
 </p>

    <% end %>